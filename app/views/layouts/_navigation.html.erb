<!-- 
  Nav bar accomodates sscreen sizes smaller than 768px
  across by collapsing into a 'burger' menu.  

  Also, the right-side content changes when users sign
  in, to offer 'Edit Profile' and 'Sign Out' instead of 
  'Sign Up' and 'Sign In'.    

  Similarly, the main nav links are pulled in from a
  navigation_links file - and are only shown if the 
  session is signed-in.    
 -->

<%# navigation styled for Bootstrap 3.0 %>
<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Shoe Biz', root_path, class: 'navbar-brand' %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <%= render 'layouts/navigation_links' %>
      </ul>
      <p class="navbar-text pull-right">
<% if signed_in? %>
        Welcome <strong><%= current_user.first_name %></strong>!
         |
        <%= link_to 'Edit Profile', edit_user_path(current_user.id), class: 'navbar-link' %>
         |
        <%= link_to "Sign Out", session_path(current_user.id), method: :delete, :class => 'navbar-link'  %>
<% else %>
        <%= link_to "Sign Up", signup_path, :class => 'navbar-link'  %>
         |
        <%= link_to "Sign In", signin_path, class: 'navbar-link'  %>
<% end %>
      </p>
    </div>
  </div>
</nav>
